{
  "name": "zipa-onnx-inference-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsx src/frame-utils.test.ts && tsx src/whisper-utils.test.ts",
    "test:mem": "playwright test tests/memory.spec.ts --reporter=line"
  },
  "dependencies": {
    "onnxruntime-web": "1.24.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
